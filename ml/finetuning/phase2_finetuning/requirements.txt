# Phase 2: Fine-Tuning Dependencies
# Requirements for PaddleOCR-VL fine-tuning with LoRA

# Core dependencies
torch>=2.0.0
transformers>=4.35.0
peft>=0.6.0  # For LoRA support
accelerate>=0.24.0
datasets>=2.14.0

# Vision and image processing
Pillow>=10.0.0
torchvision>=0.15.0
albumentations>=1.3.0  # For data augmentation
opencv-python>=4.8.0

# Configuration and utilities
PyYAML>=6.0
tqdm>=4.65.0
numpy>=1.24.0

# Evaluation and metrics
scikit-learn>=1.3.0  # For additional metrics if needed

# Logging and monitoring
tensorboard>=2.14.0
# wandb>=0.15.0  # Optional: for Weights & Biases logging

# Data handling
jsonlines>=4.0.0

# PaddleOCR-VL specific (if available via pip)
# paddleocr-vl  # Uncomment if available as package
# paddlepaddle  # Required if using PaddlePaddle directly

# Development and testing (optional)
pytest>=7.4.0
black>=23.0.0
flake8>=6.0.0

